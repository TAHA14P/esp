-- LocalScript: StarterPlayer -> StarterPlayerScripts
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")

-- ================= UNANCHOR ALL PARTS =================
local function unanchorAllParts()
	for _, obj in ipairs(Workspace:GetDescendants()) do
		if obj:IsA("BasePart") then
			obj.Anchored = false
		end
	end
end

-- Run once immediately
unanchorAllParts()

-- Optional: keep unanchoring new parts that spawn
Workspace.DescendantAdded:Connect(function(obj)
	if obj:IsA("BasePart") then
		obj.Anchored = false
	end
end)

print("All parts in the map are now unanchored!")

-- ================= CUSTOM SKY =================
local sky = Instance.new("Sky")
sky.Name = "CoolKidSky"
-- Example settings (you can replace with your preferred asset IDs)
sky.SkyboxBk = "rbxassetid://6031074895"
sky.SkyboxDn = "rbxassetid://6031074895"
sky.SkyboxFt = "rbxassetid://6031074895"
sky.SkyboxLf = "rbxassetid://6031074895"
sky.SkyboxRt = "rbxassetid://6031074895"
sky.SkyboxUp = "rbxassetid://6031074895"
sky.Parent = Lighting

print("Sky changed to CoolKidSky!")
